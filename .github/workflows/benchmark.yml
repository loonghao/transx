name: Run Benchmarks

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  deployments: write
  pull-requests: write
jobs:
  benchmark:
    name: Run pytest-benchmark (Python ${{ matrix.python-version }})
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ['3.7', '3.8', '3.9', '3.10', '3.11', '3.12']
      fail-fast: false

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install nox

    - name: Run benchmark
      run: |
        nox -s benchmark

    - name: Store benchmark result in main repo
      uses: benchmark-action/github-action-benchmark@v1
      with:
        name: TransX Performance Benchmarks (Python ${{ matrix.python-version }})
        tool: 'pytest'
        output-file-path: output.json
        github-token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        auto-push: ${{ github.event_name == 'push' }}
        gh-pages-branch: gh-pages
        alert-threshold: '200%'
        comment-on-alert: true
        fail-on-alert: true
        alert-comment-cc-users: '@longhao'
        comment-always: true
        comment-on-pull-request: true
        summary-always: true
        external-data-json-path: ./gh-pages/benchmark-data-py${{ matrix.python-version }}.json

    - name: Store benchmark result in separate repo
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: benchmark-action/github-action-benchmark@v1
      with:
        name: TransX Performance Benchmarks (Python ${{ matrix.python-version }})
        tool: 'pytest'
        output-file-path: output.json
        github-token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        auto-push: true
        gh-repository: 'github.com/longhao/transx-benchmarks'
        gh-pages-branch: main
        alert-threshold: '200%'
        comment-on-alert: true
        fail-on-alert: true
        alert-comment-cc-users: '@longhao'
